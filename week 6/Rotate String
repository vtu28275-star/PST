class Solution {
    public boolean rotateString(String s, String goal) {

        // Lengths must be equal
        if (s.length() != goal.length()) {
            return false;
        }

        // goal must be present inside s + s
        String combined = s + s;

        return combined.contains(goal);
    }
}
